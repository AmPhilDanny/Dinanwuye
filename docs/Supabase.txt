Password: [ROTATE - do not store credentials in this file]
Project URL: https://ysvqvrskwyyjbeepbyuc.supabase.co
Publisable Key: [SET IN RENDER/LOCAL ENVIRONMENT]
Render/Pooler connection string: postgresql://postgres.ysvqvrskwyyjbeepbyuc:<URL_ENCODED_PASSWORD>@aws-0-eu-west-2.pooler.supabase.com:5432/postgres?schema=dinanwuye_auth&sslmode=require

CLI setup command supabase login
supabase init
supabase link --project-ref ysvqvrskwyyjbeepbyuc

Service key: [ROTATED - set only in Render environment]

Secret Key: [ROTATED - set only in Supabase/secret manager]

Access Token: [ROTATED - never commit access tokens]

## Verified Database State (2026-08-23)

Project status: `ACTIVE_HEALTHY`

Application schemas and tables:

- `dinanwuye_auth`: `User`, `RefreshToken`, `OtpCode`
- `dinanwuye_profile`: `Profile`, `Photo`, `Preference`
- `dinanwuye_messaging`: `Conversation`, `Message`
- `dinanwuye_trust_safety`: `Block`, `Report`, `Ban`

These tables are not in `public`, so select each `dinanwuye_*` schema in Supabase Table Editor. The Supabase `auth` schema is reserved for Supabase Auth tables.

## Critical Connection Notes

- Render uses the regional pooler hostname `aws-0-eu-west-2.pooler.supabase.com`, not the direct `db.<project-ref>.supabase.co` hostname.
- The pooler username is `postgres.<project-ref>`.
- Use `sslmode=require` for Prisma. Encode `@` in the password as `%40` with no spaces.
- The repeatable application DDL is in `supabase/migrations/20260822196000_create_application_schemas.sql`.
- Demo accounts seeded for verification use password `Password123!`; rotate or remove them before any public launch.
- Never commit passwords, service keys, secret keys, or access tokens. Rotate credentials that were previously exposed in this file or deployment configuration.